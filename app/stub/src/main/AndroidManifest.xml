<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="com.android.launcher.permission.INSTALL_SHORTCUT"
        android:maxSdkVersion="25" />

    <!--
        Android 11+ hides unrelated installed packages from PackageManager.
        The hide repacker replaces this valid placeholder with the signed
        manager package while rewriting the rest of the manifest identity.
    -->
    <queries>
        <package android:name="source.reisenless.manager" />
    </queries>

    <application tools:ignore="MissingApplicationIcon">
    </application>

</manifest>
